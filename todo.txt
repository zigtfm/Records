todo

!!! ( why it excists )
bugs
	fix wrong leaderboard time recording (possible wrong >< checking)
	-- update records on new map

! ( next )
personal records cleanup
	remove P record if theres WR from the same player
add !lock
add !pw

~ ( maybe later )
personal records commands
personal records ui
loading script in tribe support
translations

? ( possible ideas )
new records history
add time offset formatting (+0.01s or -0.01s etc) in event win and in records table as possible setting
add practice mode with checkpoints, fast respawn, teleport etc.